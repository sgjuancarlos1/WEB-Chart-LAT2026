<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_product_template_chart_service" model="ir.ui.view">
        <field name="name">product.template.chart.service.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='general_information']" position="inside">
                <group string="Contratación de servicio (Chart)">
                    <field name="is_chart_service"/>
                    <field name="chart_service_periodicity" invisible="not is_chart_service"/>
                    <field name="chart_service_note" invisible="not is_chart_service"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
